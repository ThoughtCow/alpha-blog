<!DOCTYPE html>
<html>
  <head>
    <title><%= @head + "Maya Dog Walking" %></title>
    <style>
    .title {
      font-family: arial;
      font-weight: bold;
      color: white;
      font-variant: small-caps;
      }
    .caption {
      font-size: 10px;
    }
    .dog1 {
      width: 200px;
      height: auto;
    }
    .arial {
      font-family: arial;
    }
    .unbold {
      font-weight: normal;
    }
    .button {
      background-color: #4CAF50; /* Green */
      border: none;
      color: white;
      padding: 15px 32px;
      text-align: center;
      text-decoration: none;
      display: inline-block;
      font-size: 16px;
      border-radius: 4px;
    }
    </style>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% flash.each do |name, msg| %>
      <h3 style="color: green; background-color: #90EE90; font-family: arial"><b><%= msg %></b></h3>
    <% end %>
    <%= yield %>
  </body>
</html>
